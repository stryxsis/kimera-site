---
import LegalLayout from '../../layouts/LegalLayout.astro';
import { getEntry } from 'astro:content';

const locale = 'it';
const entry = await getEntry('legal', `${locale}/privacy`)!;
---

<LegalLayout locale={locale} routeKey="privacy" entry={entry} />
