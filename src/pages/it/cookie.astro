---
import LegalLayout from '../../layouts/LegalLayout.astro';
import { getEntry } from 'astro:content';

const locale = 'it';
const entry = await getEntry('legal', `${locale}/cookies`)!;
---

<LegalLayout locale={locale} routeKey="cookies" entry={entry} />
